import { compose, merge, dissoc, parseJSON, stringifyJSON } from '../'

// (object, object) => object
export const mergeState = (state, action) => parseJSON(stringifyJSON(compose(merge(state), dissoc('type'))(action)))
